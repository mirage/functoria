(jbuild_version 1)

(executables
 ((libraries (test_app alcotest cmdliner))
  (names (test))))

(alias
 ((name runtest)
  (deps (test.exe app/config.ml app/app.ml))
  (action (chdir ../../.. (run ${<} test full 1 -v)))))
